<%= form_for(@beeeother) do |f| %>
  <% if @beeeother.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@beeeother.errors.count, "error") %> prohibited this beeeother from being saved:</h2>

      <ul>
      <% @beeeother.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :reason_desc %><br />
    <%= f.text_field :reason_desc %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
