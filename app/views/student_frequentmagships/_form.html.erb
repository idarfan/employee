<%= form_for(@student_frequentmagship) do |f| %>
  <% if @student_frequentmagship.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@student_frequentmagship.errors.count, "error") %> prohibited this student_frequentmagship from being saved:</h2>

      <ul>
      <% @student_frequentmagship.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
