<%= form_for(@weeklyusage) do |f| %>
  <% if @weeklyusage.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@weeklyusage.errors.count, "error") %> prohibited this weeklyusage from being saved:</h2>

      <ul>
      <% @weeklyusage.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :reason_desc %><br />
    <%= f.text_field :reason_desc %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
