<%= form_for(@frequentmag) do |f| %>
  <% if @frequentmag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@frequentmag.errors.count, "error") %> prohibited this frequentmag from being saved:</h2>

      <ul>
      <% @frequentmag.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :reason_desc %><br />
    <%= f.text_field :reason_desc %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
