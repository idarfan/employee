<%= form_for(@student_whylearnship) do |f| %>
  <% if @student_whylearnship.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@student_whylearnship.errors.count, "error") %> prohibited this student_whylearnship from being saved:</h2>

      <ul>
      <% @student_whylearnship.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
