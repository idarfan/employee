<%= form_for(@whylearn) do |f| %>
  <% if @whylearn.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@whylearn.errors.count, "error") %> prohibited this whylearn from being saved:</h2>

      <ul>
      <% @whylearn.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :reason_desc %><br />
    <%= f.text_field :reason_desc %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
