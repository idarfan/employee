<%= form_for(@student_incomelevelship) do |f| %>
  <% if @student_incomelevelship.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@student_incomelevelship.errors.count, "error") %> prohibited this student_incomelevelship from being saved:</h2>

      <ul>
      <% @student_incomelevelship.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
