<%= form_for(@student_mostneedship) do |f| %>
  <% if @student_mostneedship.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@student_mostneedship.errors.count, "error") %> prohibited this student_mostneedship from being saved:</h2>

      <ul>
      <% @student_mostneedship.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
